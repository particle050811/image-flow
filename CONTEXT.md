# CONTEXT — 领域术语表

仅收术语与含义，不写实现细节。实现决策见 CLAUDE.md 与项目记忆。

## 术语

- **并发数**：一个任务要产出的成品条数。生图时若后端支持批量（如即梦 `generate_num`）则一次提交多张；不支持则拆成多个 job。视频上默认 1、上限 4，多条即多个 job。
- **生成方式**（视频）：用户在参数弹层为视频模型显式选择的生成模式，共五种：全能参考（默认）、图生视频、首尾帧、多帧、文生视频。与即梦网页端的模式选择对齐。
- **全能参考**：即梦视频生成模式之一，接受图片/音频/视频混合参考素材（分类型上限：图 ≤9、视频 ≤3、音频 ≤3，至少一个图或视频）。
- **参考素材**：正文 `![名](路径)` 声明的图片/音频/视频，按正文出现顺序编号（【@图片N】/【@音频N】/【@视频N】）。选定生成方式后，素材按正文顺序对号入座（如首尾帧=前两张图片）；数量或类型不匹配时报错，不静默丢弃。
- **模型注入句**：按模型内置、可在侧栏覆盖的提示词前缀。即梦各模型同样各配一条（可为空）。
- **CLI 登录态**：即梦 CLI（`dreamina`）在本机持久化的授权状态。扩展不接触任何凭据；登录只能由用户在终端手动完成（Agent 代跑 login 会产生错误授权 URL，官方已知问题）。
