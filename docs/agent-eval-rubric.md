# image-flow 评测验收标准（评分细则）

> 本文档供**评测者**使用，与 `agent-eval-spec.md`（开发需求文档）配套：把需求文档作为被测 AI Agent 的唯一输入，实现完成后按本文档逐项打分。**不要把本文档发给被测 Agent**。条款中的 § 编号均指需求文档章节。

## 评分规则（满分 100）

- 每个小项**独立计分**：做到即得分，与其他小项是否完成无关；某项只做对一半（如解析对了但替换编号错）按该项 0 分计，不再细分。
- 后续阶段不依赖前面阶段满分——例如 M2 没做侧栏，M3 的异步机制仍可通过右键命令验证计分。
- 任一阶段验收前提是 `npm run compile` 可通过（见 M1.1）；编译不过则整体只能得 M1.1 之前已得的分。

## M1 脚手架 + 端到端生成（22 分）

此阶段允许同步等待实现（不要求异步任务机制），目标是右键 MD 能出图。

| # | 验收项 | 分 |
|---|---|---|
| 1.1 | 工程可构建：`npm run compile` 零 error 零 warning（含类型检查 + lint + esbuild 双产物） | 3 |
| 1.2 | 扩展清单正确：活动栏图标 + 侧栏视图 + 两条命令 + 右键菜单仅对 `.md` 显示 | 2 |
| 1.3 | 右键纯文本 MD 能生成：调 generate 接口、轮询/等待结果、图片下载到同级 `task-*` 文件夹，文件名 `{md名}-{序号}.{扩展名}` | 5 |
| 1.4 | 参考图解析：`![alt](路径)` 读为 base64 按序随请求发送，正文替换为 `[imageN](文件名)` | 4 |
| 1.5 | 解析边界：重复引用复用同一编号；`<>` 包裹的含空格/括号路径能读回；参考图缺失时报错列出全部失败路径并中断；空 MD 报错不发请求 | 3 |
| 1.6 | 多模型尺寸字段：nano-banana 传比例+imageSize、gpt-image-2 只传比例、vip 换算像素值（如 3:4+1K → 864x1152） | 3 |
| 1.7 | 同秒连续提交落不同 `task-*` 文件夹，互不覆盖 | 2 |

## M2 侧栏化与配置系统（12 分）

| # | 验收项 | 分 |
|---|---|---|
| 2.1 | Webview 侧栏跑通：React 前端加载、三标签页可切换、CSP 含 nonce 锁脚本 | 3 |
| 2.2 | 配置读写：所有 §4.1 字段可在 UI 修改、单字段即时保存、重载窗口后仍在 | 3 |
| 2.3 | apiKey 存 secrets，不出现在 globalState | 2 |
| 2.4 | 工作台生成入口可用：显示生效 MD 文件名、无 Key 时提示、生成期间按钮禁用 | 2 |
| 2.5 | 任务页历史：扫描生效 MD 同级 `task-*` 文件夹倒序展示缩略图（asWebviewUri），点击打开原图，无图文件夹不列出 | 2 |

## M3 异步任务机制（18 分）

| # | 验收项 | 分 |
|---|---|---|
| 3.1 | `replyType:'async'` 提交拿 job id，后台轮询出结果，不阻塞 UI | 4 |
| 3.2 | 并发：并发数 N 一次提交 N 个 job，图片落同一文件夹接续编号不重名 | 3 |
| 3.3 | 单定时器轮询：所有任务共用一个定时器，无 running job 时停止；轮询有重入锁 | 3 |
| 3.4 | 持久化 + 重启续拉：任务存 globalState，重载窗口后卡片恢复并继续轮询到完成 | 4 |
| 3.5 | 续拉细节：超时按会话起算（离线超 10 分钟不误判）；submitting 态无 id 的 job 重启即判失败不卡死 | 2 |
| 3.6 | 终结处理：全部 job 终结后从持久化移除；失败按「有成图→警告 / 无成图→错误」弹通知，错误去重聚合 | 2 |

## M4 素材库（12 分）

| # | 验收项 | 分 |
|---|---|---|
| 4.1 | 手动库：文件夹选择器添加（去重）、可移除、递归扫描展示、列表存 workspaceState | 3 |
| 4.2 | 扫描保护：递归深度 ≤3、条目 ≤500 | 1 |
| 4.3 | 自动库：按生效 MD 路径从工作区根下一层到 MD 所在层逐层成库，仅取各层直接图片，无图层不显示 | 3 |
| 4.4 | 右键插入引用：相对路径 + `./` 前缀 + alt 取文件名，插到生效 MD 光标处 | 3 |
| 4.5 | 插入边界：含空格/括号路径自动 `<>` 包裹；活动编辑器非生效 MD 时拒绝；跨盘符警告放弃 | 2 |

## M5 预览请求（8 分）

| # | 验收项 | 分 |
|---|---|---|
| 5.1 | 预览命令/按钮打开预览文档，不调 API；含提示词、请求地址、参数 JSON、参考图概览四段 | 4 |
| 5.2 | 参数 JSON 剔除 prompt/images；参考图概览为「前 48 字符…（总长）」格式 | 2 |
| 5.3 | 预览与真实提交共用同一请求体构造逻辑（改模型为 vip 后预览中 aspectRatio 变像素值即可验证） | 2 |

## M6 提示词注入（10 分）

| # | 验收项 | 分 |
|---|---|---|
| 6.1 | 最终 prompt = 模型注入句 + IMAGES.md + 正文，空行连接、空段省略；IMAGES.md 缺失静默跳过 | 3 |
| 6.2 | 提交与预览两处都生效（预览可见注入内容） | 2 |
| 6.3 | 设置页可按模型编辑注入句（模型下拉 + 文本框，与工作台模型选择解耦） | 2 |
| 6.4 | 种子机制：gpt-image 系列首次激活种入抑噪句；用户改动（含清空）后不再被覆盖；激活时先种入再注册侧栏（首读即可见） | 3 |

## M7 体验打磨（10 分）

| # | 验收项 | 分 |
|---|---|---|
| 7.1 | 后台提交：点生成立即出现卡片（提交中 x/N），不等网络往返；全部提交失败则卡片移除并弹错 | 3 |
| 7.2 | 进行中卡片：聚合进度条（按 §8.4 公式）+ 逐秒计时（真实提交时间起算，重启不重置）+ 已成图实时追加 | 3 |
| 7.3 | 生效 MD 跟随活动编辑器；切到非 MD 标签（图片详情/预览）保留不清空 | 2 |
| 7.4 | 任务/历史合并为按文件夹名倒序的一条列表；历史排除进行中文件夹不重复；完成瞬间不闪烁（先并入历史再移卡） | 2 |

## M8 健壮性与测试（8 分）

| # | 验收项 | 分 |
|---|---|---|
| 8.1 | 接口响应运行时校验（status 枚举 + 字段形状），格式异常判失败不裸断言 | 2 |
| 8.2 | 瞬时错误处理：5xx/429/超时/网络失败保持 running 下轮重试，非瞬时错误直接判失败；请求全部带超时 | 2 |
| 8.3 | 无成图任务的空 `task-*` 文件夹被清理（提交全败与轮询终结两条路径都覆盖） | 1 |
| 8.4 | §12 列出的单元测试齐全且 `npm test` 通过 | 3 |

## 端到端冒烟清单（完成度初筛）

逐项打分用上面的评分细则；以下 12 条快速冒烟检查用于初筛，全过则大概率拿到高分：

1. `npm run compile` 零 error 零 warning 通过（含两套类型检查与 lint）。
2. F5 启动后：活动栏出现图标，侧栏三个标签页可用，配置改动即存、重载窗口后仍在。
3. apiKey 不出现在 globalState 中（存 secrets）。
4. 对含两张参考图（其中一张路径带空格用 `<>` 包裹）的 MD 点「预览请求」：prompt 中图片语法被替换为 `[image1]`/`[image2]`，参考图概览两条，请求参数无 prompt/images 字段。
5. 并发数设 3 点生成：立即出现进行中卡片（提交中 3/3），按钮几乎不阻塞；卡片展示进度条与逐秒计时；完成后图片落在同一 `task-*` 文件夹且文件名连续编号不覆盖。
6. 生成进行中重启 VS Code（或重载窗口）：任务卡片恢复并继续轮询直到完成。
7. 任务全部失败：弹错误通知、卡片消失、空 task 文件夹被清理。
8. 历史页正确列出既有 task-* 文件夹，进行中任务不重复出现。
9. 素材库：手动添加文件夹可递归显示图片；打开子目录中的 MD 时「当前路径」逐层出现自动库；右键缩略图把 `![alt](./相对路径)` 插入光标处，含空格路径自动加 `<>`；活动编辑器不是生效 MD 时拒绝插入。
10. 切换模型为 `gpt-image-2-vip` 后预览请求中 `aspectRatio` 为像素值（如 `864x1152`）。
11. 工作区根放置 IMAGES.md 后，预览的 prompt 前部包含其全文与模型注入句。
12. 单元测试全部通过。
